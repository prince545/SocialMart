name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
    - uses: actions/checkout@v3
    
    # Server Build
    - name: Use Node.js ${{ matrix.node-version }} for Server
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: server/package-lock.json
    - name: Install Server Dependencies
      run: |
        cd server
        npm ci
    # - name: Run Server Tests
    #   run: |
    #     cd server
    #     npm test

    # Client Build
    - name: Use Node.js ${{ matrix.node-version }} for Client
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: client/package-lock.json
    - name: Install Client Dependencies
      run: |
        cd client
        npm ci
    - name: Build Client
      run: |
        cd client
        npm run build

  # Docker push job would go here (requires secrets)
  # push-to-registry:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v2
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #     
  #     - name: Build and push Server image
  #       uses: docker/build-push-action@v4
  #       with:
  #         context: ./server
  #         push: true
  #         tags: user/socialmart-server:latest
  #
  #     - name: Build and push Client image
  #       uses: docker/build-push-action@v4
  #       with:
  #         context: ./client
  #         push: true
  #         tags: user/socialmart-client:latest
